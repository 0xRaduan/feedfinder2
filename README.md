# Feedfinder2

This package helps locate RSS/Atom feeds for a given website.

Requires Python 3.10 or later.

## Poetry compatibility

Ensure you use version 0.0.4 or later. Older revisions included a
`backend-path` entry in `pyproject.toml` that prevented Poetry from
building the project.
